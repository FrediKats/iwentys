@using Iwentys.Features.Guilds.Models.Guilds
@namespace Iwentys.Endpoint.Client.Pages.Guilds

<MatPaper Elevation="10" Rounded="true" class="text-center">
    <MatHeadline5>Рейтинг гильдии - @GuildLeaderboard.TotalRate</MatHeadline5>
    <MatHeadline6>Импакт:</MatHeadline6>
    <MatList>
        @foreach (GuildMemberImpactDto member in GuildLeaderboard.MembersImpact)
        {
            <MatListItem>
                @member.Username (@member.TotalRate)
            </MatListItem>
        }
    </MatList>
</MatPaper>

@code {
    [Parameter]
    public GuildMemberLeaderBoardDto GuildLeaderboard { get; set; }
}